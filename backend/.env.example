# =============================================================
# PLANTILLA DE VARIABLES DE ENTORNO
# Copia este archivo como .env.local o .env.production y rellena
# =============================================================

# --- Base de Datos (Postgres) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=betting_db
DATABASE_URL=postgres://postgres:password@localhost:5432/betting_db

# --- Cache (Redis Local TCP) ---
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# --- Upstash Redis (Solo Producción - REST sobre HTTPS) ---
# Si estas dos variables existen, la app usa Upstash automáticamente.
# UPSTASH_REDIS_REST_URL=https://your-id.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token-here

# --- Aplicación ---
APP_ENVIRONMENT=local
APP_PORT=8000
APP_HOST=127.0.0.1
